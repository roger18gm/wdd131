const PI = 3.14;
let area = 0;

function circleArea(radius) {
    // code to complete our task here
    const area = radius * PI;
    return area
  }

area = circleArea(3);
console.log(area)

//inspect >> console >> ">>" >> sources >> breakpoints, step in, step out